<article>
    <h1><%= @post.title %></h1>
    <time datetime="<%= @post.published.strftime("%Y-%m-%d") %>">
        <%= @post.published.strftime("%B %d, %Y") %>
    </time>
    <%= raw @post.content %>
</article>